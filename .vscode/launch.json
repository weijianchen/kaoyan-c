{
    "configurations": [
        {
            "name": "使用codelldb调试当前c文件",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/${fileBasenameNoExtension}",
            "args": [],
            "stopOnEntry": false,
            "cwd": "${workspaceFolder}",
            "terminal": "integrated",
            "preLaunchTask": "mac环境编译当前c文件而不是构建项目同时编译多个源文件至一个可执行文件"
        }
    ],
    "version": "2.0.0"
}