#include <stdio.h>
#include "string.h"

void undo(void)
{
    //空函数
}

int main(void)
{
    char c[] = "demo";//字符数组存储字符串
    printf("hello, %s", c);
    int x;
    scanf("%d", &x);
    printf("the value you input is: %d", x);
}
